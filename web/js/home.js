jQuery(function () {

    jQuery('.carousel')
        .on('carouselComplete', function () {
            debugger;
        })
        .on('click', 'a', function (e) {
            //e.preventDefault();

        });

});
